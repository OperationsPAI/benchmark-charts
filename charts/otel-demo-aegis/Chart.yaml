apiVersion: v2
name: otel-demo-aegis
description: opentelemetry-demo + aegis-cluster OTel shim (wrapper chart). Routes app OTLP to the cluster collector instead of the demo's in-chart collector.
version: 0.1.0
appVersion: "2.1.3"
dependencies:
- name: opentelemetry-demo
  version: 0.38.1
  repository: ""
