# HotelReservation — DSB Go stack.
SYS=hs
FORK_URL=https://github.com/LGU-SE-Internal/DeathStarBench.git
FORK_DIR=/tmp/dsb-lgu
SUBPATH=hotelReservation

# Image tuple format: "<docker-name>:<Dockerfile-path>:<build-context>"
# Paths are relative to FORK_DIR.
IMAGES=(
  "hotelreservation:hotelReservation/Dockerfile:hotelReservation"
  "hotelreservation-loader:hotelReservation/Dockerfile-loader:."
)
